# Gated pages (dashboard, workshops, admin…) are excluded from search via a
# per-route noindex meta tag (src/constants/seo.js), not Disallow rules — a
# disallowed page can't be crawled, so its noindex would never be seen.
#
# The host below must be the CANONICAL one: https://www.tcfpasserelle.com.
# The apex (tcfpasserelle.com) answers 308 -> www, so naming the apex here
# would send every crawler through a redirect. `npm run check:seo` verifies
# this file, index.html and sitemap.xml all agree on that host.
User-agent: *
Allow: /

Sitemap: https://www.tcfpasserelle.com/sitemap.xml
